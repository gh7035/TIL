# useState란?

말 그대로 쓴다 + 상태 다. <p>
상태라는 변수의 초기값을 설정하고, 이후에 변경할수 있게 도와주는 함수이다.

```js


const [user, setUser] = useState(user)

// 해석:
const [상태설정을 희밍하는 객체, 변경할때 쓰일 메서드명] = useState(초기값 지정)

```

사용한 예시:

```js

type Mode = "black" | "white"

const [mode, setMode] = useState<Mode>("black")


```

